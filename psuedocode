trivia game 


create object captial with keys with the country name 
example :
 var capital = {Nepal : 'Kathmandu', India : 'New Delhi', England : 'London', USA :'Washington DC'}

 create and array to store the Keys (i.e the Country Name)

 

reset function (
        
        Var country = Object.keys(capital) // need to splice later 

        random geration of question :
            var questionCountry = country[MATH.Floor(Math.random() * country.length)]
            country.splice(questionCountry,1); // 


        3. random answers generator ( 1 has to be the right answer)
        create an array for options

        var options = [0,1,2,3]
        // 1. insert the right answer 
        option[0] = capital[questionCountry] // correct answer
        option[1] = captial [MATH.Floor(Math.random() * country.length)]] // these can generate same values need avoid similar options
            country.splice(option[1],1);
        option[2] = captial [MATH.Floor(Math.random() * country.length)]] // these can generate same values need avoid similar options
            country.splice(option[2],1);
        option[3] = captial [MATH.Floor(Math.random() * country.length)]] // these can generate same values need avoid similar options
            country.splice(option[3],1);

        create  in random order
        var optionTemp =[]
            for (var i =0; i < option.length; i++){
                var j = MATH.Floor(Math.random() * option.length)]
                optionTemp.push(option[j])
                option.splice(j,1)
            }

    display();
)


display (

// display grabbing the Id from html 
 1.  'What is the capital of ' + questionCountry
 
    'a.' + optionTemp[0]
    'b.' + optionTemp[1]
    'c.' + optionTemp[2]
    'd.' + optionTemp[3]

)